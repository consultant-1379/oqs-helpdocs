define({
    defaultApp: 'helpdocs',
    name: 'OpenStack Queuing Solution',
    properties: {
        '*': {
            i18n: {
                // global override, forces the locales for all packages
                // use with caution !
                locales: [
                    'en-us'
                ]
            }
        }
    }
});
